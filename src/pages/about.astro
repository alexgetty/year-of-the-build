---
// TODO: Uncomment when @gettymade/construct is published to npm
// import '@gettymade/construct/styles';
import '../styles/theme.css';
import { SiteHeader } from '../components/SiteHeader';
import { SiteFooter } from '../components/SiteFooter';
import { PageHeader } from '../components/PageHeader';
import Breadcrumbs from '../components/Breadcrumbs.astro';
import { copy } from '../lib/copy';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={`About - ${copy.site.tagline}`} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>About - Year of the Build</title>
  </head>
  <body>
    <SiteHeader client:load variant="light" />

    <Breadcrumbs paths={[{ label: 'About' }]} />

    <main class="main-content">
      <PageHeader
        title={copy.about.headline}
        description="Follow along as I embark on a year of building."
      />

      <article class="article-content">
        <div class="container">
          <div class="prose">
            <p>
              Year of the Build started as a challenge to myself: stop tinkering in private and actually ship things. The devlogs are accountability. If I write about what I'm working on, I'm more likely to finish it.
            </p>
            <p>
              That's it. No grand philosophy. Just building, launching, and writing about what I learn along the way.
            </p>
          </div>
        </div>
      </article>
    </main>

    <SiteFooter />
  </body>
</html>

<style>
  body {
    margin: 0;
    padding: 0;
    padding-top: var(--header-height, 60px);
  }

  .container {
    max-width: var(--yotb-content-max);
    margin: 0 auto;
    padding: 0 var(--t-spacing-lg);
  }

  .article-content {
    padding: var(--t-spacing-xl) 0 var(--t-spacing-2xl);
  }

  .prose {
    max-width: 640px;
  }

  .prose p {
    font-size: var(--t-font-size-base);
    line-height: var(--t-line-height-relaxed);
    color: var(--text-secondary);
    margin-bottom: var(--t-spacing-md);
  }

  .prose p:last-child {
    margin-bottom: 0;
  }

  @media (max-width: 600px) {
    .container {
      padding: 0 var(--t-spacing-md);
    }

    .article-content {
      padding: var(--t-spacing-md) 0 var(--t-spacing-lg);
    }
  }
</style>
